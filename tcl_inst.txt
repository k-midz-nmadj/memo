[TinyCoreLinux SSHサーバ インストール手順]

<CD boot>
#tce-load -wi tc-install.tcz
#sudo tc-install.sh
Install from [R]unning OS, from booted [C]drom, ...: [C]CD-ROMからインストールを選択
Select Install Type [F]rugal, [H]DD, [Z]ip.: インストール種別は[H]HDDを選択
Select disk for core: インストール先ディスクは[1]sdaを選択
Select Formatting Option for sda1: ファイルシステムは[3]ext4を選択
Enter space separated boot options:
ブートオプション指定で、"home=sda1 opt=sda1 tce=sda1 host=ホスト名 norestore noautologin" を1行で入力
#sudo poweroff

<HDD boot>
#tce-loda -wi openssh
#mkdir -p /opt/etc/ssh
#cd /opt/etc/ssh
#cp /usr/local/etc/ssh/sshd_config.orig sshd_config
#ssh-keygen -t ed25519 -f ssh_host_ed25519_key

#sudo passwd tc
#cd /opt
#sudo cp /etc/shadow etc/shadow

echo "sudo cp -f /opt/etc/shadow /etc/shadow" >> bootlocal.sh
echo "/usr/local/sbin/sshd -f /opt/etc/ssh/sshd_config -h /opt/etc/ssh/ssh_host_ed25519_key &" >> bootlocal.sh
